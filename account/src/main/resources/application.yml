spring:
  profiles:
    active: dev
  jpa:
    database: mysql
Account:
  SecretKey: ThisIsASecretKeyValue

---
spring:
  profiles: dev
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

---
spring:
  profiles: prod
  jpa:
    hibernate:
      ddl-auto: validate